# DesignPatterns
In this assignment the design patterns used were DAO data access object and Factory factor of connection. 
There is only one class responsible for SQL, otherwise only one class instantiate the connection with the database.  
The class Country and DAO is responsible to make all communication with the database.
